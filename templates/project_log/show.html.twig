{% extends 'base.html.twig' %}

{% block title %}ProjectLog{% endblock %}

{% block body %}
    <h1>ProjectLog</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ project_log.id }}</td>
            </tr>
            <tr>
                <th>Action</th>
                <td>{{ project_log.action }}</td>
            </tr>
            <tr>
                <th>PerformedAt</th>
                <td>{{ project_log.performedAt ? project_log.performedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_project_log_index') }}">back to list</a>

    <a href="{{ path('app_project_log_edit', {'id': project_log.id}) }}">edit</a>

    {{ include('project_log/_delete_form.html.twig') }}
{% endblock %}
