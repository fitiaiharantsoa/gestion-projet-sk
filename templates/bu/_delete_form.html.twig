<form method="post" action="{{ path('app_b_u_delete', {'id': b_u.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ b_u.id) }}">
    <button class="btn">Delete</button>
</form>
