{{ form_start(form) }}
    {{ form_row(form.type) }}
    {{ form_row(form.dateUpload) }}
    {{ form_row(form.projet) }}

    <button class="btn btn-success">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
